# Runtime dependencies.
#
# Version ranges rather than exact pins: this file is what the container image
# installs, and a lock file is produced from it. Regenerate the lock with
# `make lock` before a production deployment so that a rebuild is reproducible.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
sqlalchemy[asyncio]>=2.0.30,<3.0
asyncpg>=0.29,<0.31
alembic>=1.13,<2.0
httpx>=0.27,<0.29
structlog>=24.1,<26.0
